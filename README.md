# MeshCore WebUI

Self-hostable web client for MeshCore LoRa mesh devices over WiFi/TCP. Includes iOS PWA push notifications via Web Push + VAPID.

**Status:** implementation in progress — see `docs/plans/`.
